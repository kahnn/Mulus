---------------------------------------------------------
- 実装上のTODO

コマンドでのIF指定を無いか、任意に'指定可能にする
クロス環境向けなど、Makefileの整理
コマンド実行はシリアライズする必要があるので、制約？として明記する。

ログの取り扱いを整理する。(全体的に統一したエラー出力機構)
パケットの取り扱いを容易にするため、bytebuffer機能を取り入れてソース整理する。
コマンドで使うマルチキャスト用のインターフェース(eth0など)を引数指定可能にする。
mls_el_ctx などtermでのリソース解放ができていないところがあるので、対応する
デバイスオブジェクトは、テンプレート型式で生成できるようにする。それをベースに各自が開発する（予定）。
el-discover を複数ノードを取得可能にする
el-prop で複数プロパティを取り扱えるようにする
テストが不十分なので追加する
オブジェクト内容出力コマンドの追加
コントローラ向け機能の追加
  -> OPEを拡張するか?
el_prop向け(unicast)向けに、ローカルポートの任意指定を可能にする。
開発・テスト環境を記述する
設定機能の導入
setget対応
インスタンスID==0への対応
IPv6対応

−−−−−−−−
ロードマップを記述する
アルファ版で機能の作り込み（外部IFの作り込み）
ベータ版で内部IFの作り込み
オブジェクトテンプレートの導入
デバッグ
正式版のリリース
−−−−−−−−

---------------------------------------------------------
- リリース作業手順

- README.md のリリース記録
- タグを付ける
  * RELEASE-0.0.0_ALPHA-1 など
  git tag 
- 内容を push する

* github project description 説明更新
 Project Mulus is ECHONET Lite Tool-kit, which is implemented in the C-language.
